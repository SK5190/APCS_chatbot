{
  "name": "APCS -chatbot",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd ../Frontend && npm ci && npm run build && cd ../Backend && npm ci",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Render: root Backend, build npm install && npm run build",
  "dependencies": {
    "@google/genai": "^1.17.0",
    "bad-words": "^4.0.0",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^8.0.4",
    "socket.io": "^4.8.1"
  }
}
